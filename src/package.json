{
  "name": "free-manga-site-src",
  "version": "1.0.0",
  "description": "use this only to run both backend and frontend concurrently",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "author": "22CLC05 - Group 01",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}